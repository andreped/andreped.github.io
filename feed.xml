<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.2.2">Jekyll</generator><link href="https://andreped.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://andreped.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2022-06-05T13:56:58+00:00</updated><id>https://andreped.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design.
</subtitle><entry><title type="html">setting up multi-cuda environments</title><link href="https://andreped.github.io/blog/2022/multi-cuda/" rel="alternate" type="text/html" title="setting up multi-cuda environments" /><published>2022-01-08T16:30:00+00:00</published><updated>2022-01-08T16:30:00+00:00</updated><id>https://andreped.github.io/blog/2022/multi-cuda</id><content type="html" xml:base="https://andreped.github.io/blog/2022/multi-cuda/"><![CDATA[<h2 id="motivation">Motivation</h2>
<p>Working with machine learning generally requires that you have access to a dedicated GPU to enable enable rapid training and inference of models. Either if you are using TensorFlow, PyTorch, or any other framework, you will find that the library has dependencies, and often specific versions.</p>

<p>For instance, if you wish to use <a href="https://github.com/andreped/PLS-Net">this</a> implementation of the PLS-Net architecture, it requires PyTorch 1.3.0. It might work with more recent version of PyTorch, but there are no guarantees. This is annoying, as you might be using a more recent version of PyTorch in other projects. Luckily, using different versions (and libraries) for specific applications can easily be done by creating separate virtual environments, like so:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ pip install virtualenv
$ virtualenv -ppython3 venv --clear
$ source venv/bin/activate  # on UNIX
or 
$ ./venv/Scripts/activate  # on Windows
</code></pre></div></div>

<p>However, when you try to setup PyTorch or TensorFlow you realize that the framework fails to find any GPUs. This might be because the version you are using is incompatible with the CUDA and/or cudNN versions you have on your machine. Using a specific version of a library requires a very specific set of dependencies, and not just to CUDA (more information regarding dependencies can be found <a href="https://www.tensorflow.org/install/source#gpu">here</a>).</p>

<p>Therefore, if you wish to use PyTorch 1.3.1, it might require you to use CUDA 10.0, but sadly, it is not compatible with the CUDA 11.1 version your PyTorch 1.7.0 requires.</p>

<p>However, note that multiple versions of PyTorch/TensorFlow might be compatible with multiple CUDA versions. Therefore, try that first before trying any of the suggested solutions below! A compatility chart for TensorFlow-CUDA can be found <a href="https://www.tensorflow.org/install/source#gpu">here</a>.</p>

<h2 id="alternative-solutions">Alternative solutions</h2>
<p>A simple solution to this can be to build PyTorch from scratch with the CUDA version you have, and that might work in many cases. However, in general, building libraries from scratch is a tedious process and might require you to make quite a bit of effort to get it to work. So for all us <code class="language-plaintext highlighter-rouge">pip install torch</code> people out there, we prefer to avoid this.</p>

<p>Therefore, not only do you need to create a virtual environment, but you also need to have a specific CUDA version with that environment. That is <strong>not</strong> possible to do using only virtualenv, as the CUDA version is (most commonly) set globally on your machine and not inside the virtual environment.</p>

<p>Creating a docker image that has that specific CUDA version preinstalled is an alternative, and perhaps the preferred solution. However, there is a lot of ML developers that are not experienced with docker or know how to set it up (to be discussed in a future blog post maybe?), and it requires a lot more work than the basic virtualenv solution.</p>

<h2 id="the-solution">The solution</h2>
<p>Therefore, the solution might be to have multiple CUDA version on the same device and simply toggle between them for each session, depending on which CUDA version you need at a given time.</p>

<p>A good tutorial for how to do this is found <a href="https://www.pugetsystems.com/labs/hpc/How-to-install-CUDA-9-2-on-Ubuntu-18-04-1184/">here</a> for CUDA 10.0 and CUDA 9.2 (on Ubuntu Linux 18.04). But the most important parts, which I struggled with when trying to setup multi-CUDA environments myself were the following steps:</p>

<ul>
  <li>Make a file called “cuda9.2-env” and add the following:
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; export PATH=$PATH:/usr/local/cuda-9.2/bin
&gt; export CUDADIR=/usr/local/cuda-9.2
&gt; export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/cuda-9.2/lib64
</code></pre></div>    </div>
  </li>
  <li>Then to switch between the specified CUDA version, run:
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; source path_to_file/cuda9.2-env
</code></pre></div>    </div>
  </li>
</ul>

<p>For different CUDA versions simply make different “cudaX.Y-env” files.</p>

<p>That’s it! Now it is possible to use different CUDA version on the same platform.</p>

<p>What is essentially done here is that you set which CUDA version is used in the environmental variables, and therefore, PyTorch/TensorFlow is able to find the CUDA version you want to use, because it uses the environmental variables when creating the session.</p>

<p>But of course, for all you lazy people that want an even easier solution, simply make an alias to make toggling between these more easily. For example:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; alias cuda9.2-env="source /home/username/cuda_environments/cuda9.2-env"
&gt; alias cuda10.0-env="source /home/username/cuda_environments/cuda10-0-env"
</code></pre></div></div>

<h2 id="concluding-remarks">Concluding remarks</h2>
<p>And that’s it. No magic really. I have done this for multiple projects with no real issue. But if you have some experience with docker, or are interesting in learning about it, I would recommend doing that instead, as docker images not only handled dependencies relevant for training, but it makes <strong>deployment</strong> of trained models and implemented solutions directly available on new devices and across operating systems!</p>

<p>I hope you found this helpful :]</p>]]></content><author><name></name></author><category term="machine-learning" /><category term="environments" /><category term="CUDA" /><category term="PyTorch" /><category term="TensorFlow" /><summary type="html"><![CDATA[a quick tutorial on how to setup multiple CUDA versions on the same Linux machine and toggle between them.]]></summary></entry></feed>